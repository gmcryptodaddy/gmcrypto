# robots.txt for gmcrypto.news
# Allow all search engines to crawl the entire site.

User-agent: *
Allow: /

# Block crawling of API routes and dynamic-only filtered URLs
# (filtered homepage URLs use canonical/noindex tags so they don't dilute SEO)
Disallow: /api/

# Sitemap locations
Sitemap: https://www.gmcrypto.news/sitemap.xml
Sitemap: https://www.gmcrypto.news/sitemap-news.xml

# RSS feed (auto-discoverable via <link rel="alternate"> in <head>)
# https://www.gmcrypto.news/feed.xml
